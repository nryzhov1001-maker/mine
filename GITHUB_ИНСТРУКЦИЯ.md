# 🚀 Инструкция по загрузке на GitHub

## Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Заполните:
   - **Repository name:** `zvxchain-1.12.2` (или другое название)
   - **Description:** "Декоративные цепи для Minecraft 1.12.2"
   - **Public** (чтобы GitHub Actions работал бесплатно)
   - ✅ НЕ ставьте галочку "Add a README file"
   - ✅ НЕ добавляйте .gitignore (он уже есть)
3. Нажмите **"Create repository"**

## Шаг 2: Загрузите файлы

### Вариант А: Через веб-интерфейс GitHub (самый простой)

1. На странице созданного репозитория нажмите **"uploading an existing file"**
2. Перетащите ВСЕ файлы и папки из `zvxchain_1.12.2/` в окно браузера:
   - `.github/` (папка)
   - `gradle/` (папка)
   - `src/` (папка)
   - `.gitignore`
   - `build.gradle`
   - `gradle.properties`
   - `gradlew`
   - `gradlew.bat`
   - `README.md`
3. Напишите commit message: "Initial commit"
4. Нажмите **"Commit changes"**

### Вариант Б: Через Git командную строку

```bash
cd zvxchain_1.12.2

# Инициализация Git
git init
git add .
git commit -m "Initial commit"

# Подключение к GitHub (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/zvxchain-1.12.2.git
git branch -M main
git push -u origin main
```

### Вариант В: Через GitHub Desktop

1. Откройте GitHub Desktop
2. File → Add local repository
3. Выберите папку `zvxchain_1.12.2`
4. Нажмите "Create repository"
5. Publish repository → снимите галочку "Keep this code private"
6. Нажмите "Publish repository"

## Шаг 3: Дождитесь сборки

1. Перейдите во вкладку **"Actions"** вашего репозитория
2. Вы увидите процесс сборки "Build Minecraft Mod"
3. Подождите ~5-10 минут (зелёная галочка = успех)
4. Кликните на завершённый build
5. Внизу страницы найдите **"Artifacts"**
6. Скачайте **"ZVXchain-1.12.2.zip"**
7. Распакуйте - внутри будет готовый JAR файл!

## Шаг 4: Установите мод

1. Распакуйте скачанный ZIP
2. Скопируйте `.jar` файл в `.minecraft/mods/`
3. Запустите Minecraft 1.12.2 с Forge
4. Готово! ✅

---

## 📋 Что загружать на GitHub:

Из папки `zvxchain_1.12.2/` нужно загрузить:

```
zvxchain-1.12.2/               (корень репозитория)
├── .github/
│   └── workflows/
│       └── build.yml          ← Скрипт автосборки
├── gradle/
│   └── wrapper/
│       ├── gradle-wrapper.jar
│       └── gradle-wrapper.properties
├── src/
│   └── main/
│       ├── java/              ← Исходный код
│       └── resources/         ← Текстуры, модели, рецепты
├── .gitignore                 ← Исключения для Git
├── build.gradle               ← Конфигурация сборки
├── gradle.properties          ← Настройки Gradle
├── gradlew                    ← Gradle wrapper (Linux/Mac)
├── gradlew.bat                ← Gradle wrapper (Windows)
└── README.md                  ← Описание проекта
```

**НЕ загружайте:**
- ❌ `build/` (создастся при сборке)
- ❌ `.gradle/` (временные файлы)
- ❌ `.idea/`, `.project` (IDE файлы)
- ❌ `*.jar` файлы из корня (кроме gradle-wrapper.jar)
- ❌ `temp_jar/`, `final_jar/` (временные папки)

Все исключения уже прописаны в `.gitignore`!

---

## 🔧 Устранение проблем

### Если сборка не запускается:
1. Проверьте, что репозиторий **Public**
2. Перейдите в Settings → Actions → General
3. Выберите "Allow all actions"
4. Сохраните

### Если сборка завершается с ошибкой:
1. Кликните на failed build
2. Посмотрите логи
3. Обычно помогает: повторный запуск (Re-run jobs)

### Если нет вкладки Actions:
- Репозиторий должен быть Public
- Или включите Actions в Settings → Actions

---

## ✅ После успешной сборки

Можете создать Release:
1. Releases → Create a new release
2. Tag: `v2.1`
3. Title: "ZVXchain 2.1 для Minecraft 1.12.2"
4. Прикрепите скачанный JAR
5. Publish release

Теперь мод можно скачивать напрямую из Releases!

---

**Удачи!** После загрузки на GitHub вы получите готовый рабочий JAR через 5-10 минут! 🎉

